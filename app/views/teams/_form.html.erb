<div class="field">
  <div class="control">
    <label class="label">Team Name</label>
    <input type="text" class="input" />
  </div>
</div>

<div class="content">
  <h2 class="title is-4">Invite users</h2>
  <p>Invite users to create an account and join this team. Each user will be sent a confirmation email in whic h they can supply their own unique password</p>
</div>

<div class="field">
  <div class="control">
    <label for="" class="label">Name</label>
    <input type="text" name="" id="" />
  </div>
</div>

<div class="field">
  <div class="control">
    <label for="" class="label">Email</label>
    <input type="email" name="" id="" />
  </div>
</div>

<input type="hidden" value="<%= current_user.id %>" />

<div class="field">
  <div class="control">
    <button class="button is-small">Remove</button>
  </div>
</div>

<hr />

<div class="field">
  <div class="control">
    <% if current_page?(new_team_path) %>
      <button class="button is-success is-large">Create Team</button>
    <% else %>
      <button class="button is-success is-large">Savee Team</button>
    <% end %>
  </div>
</div>